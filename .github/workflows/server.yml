name: Dedicated server readiness

on:
  workflow_call:
    inputs:
      artifact-name:
        description: Uploaded mod JAR artifact to test
        required: true
        type: string
      fixture:
        description: Comma-separated dependency fixture profiles
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  server:
    name: Dedicated server
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Set up dependencies
        uses: bertie-mc/bertie-ci/actions/setup-nix@v3.1.0

      - name: Download built mod
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v8
        with:
          name: ${{ inputs.artifact-name }}
          path: .bertie-ci/artifact

      - name: Test server
        uses: bertie-mc/bertie-ci/actions/server@v3.1.0
        with:
          fixture: ${{ inputs.fixture }}

      - name: Upload server logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: minecraft-server-${{ github.repository_id }}-${{ github.sha }}
          path: |
            .bertie-ci/server/**/*.log
            .bertie-ci/server/**/crash-reports/
          if-no-files-found: warn
          retention-days: 14
